shiny:
  image:
    repository: inseefrlab/template-shiny-app
    tag: "1.0.0"
  ingress:
    enabled: true
    annotations:
      kubernetes.io/ingress.class: nginx
    hosts: 
      - host: myshinyapp.lab.sspcloud.fr
        paths:
        - path: /
          pathType: Prefix
  s3:
    enabled: true
    existingSecret: myshinyapp-s3
  postgresql:
    enabled: true
    auth:
      username: myusername
      database: defaultdb
      existingSecret: myshinyapp-postgresql
    containerPorts:
      postgresql: 5432
    fullnameOverride: postgresql-db
